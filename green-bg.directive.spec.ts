import { GreenBgDirective } from './green-bg.directive';

describe('GreenBgDirective', () => {
  it('should create an instance', () => {
    const directive = new GreenBgDirective();
    expect(directive).toBeTruthy();
  });
});
