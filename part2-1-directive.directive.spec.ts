import { Part21DirectiveDirective } from './part2-1-directive.directive';

describe('Part21DirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new Part21DirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
