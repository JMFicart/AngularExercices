import { ChildTextDirectiveDirective } from './child-text-directive.directive';

describe('ChildTextDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ChildTextDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
